# Docs

Quick notes for using this repo.

## Pattern Cues

| Prompt clue | Pattern to try |
| --- | --- |
| sorted input | binary search or two pointers |
| pair, target, complement | hash map |
| longest or shortest contiguous range | sliding window |
| counts, duplicates, groups | hash map or frequency map |
| matching or nested items | stack |
| top k or kth largest/smallest | heap |
| tree height, path, ancestor | DFS |
| levels or shortest unweighted path | BFS |
| connected components | DFS, BFS, or Union Find |
| dependencies or ordering | topological sort |
| subsets, combinations, permutations | backtracking |
| best/min/max after choices | dynamic programming |
| prefixes or autocomplete | trie |
| xor, bits, no arithmetic | bit manipulation |

## Practice Tracker

Use this format in your notes:

```text
Now:
- arrays_hashing/two_sum

Review:
- problems solved with hints or slow attempts

Done:
- problems solved cleanly on a later day
```

After each problem, write:

- pattern
- time complexity
- space complexity
