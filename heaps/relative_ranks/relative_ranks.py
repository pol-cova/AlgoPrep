from typing import List

class RelativeRanks:
    def find_relative_ranks(self, score: List[int]) -> List[str]:
        """
        You receive an array of scores, where the value at index i corresponds
        to the score of the i-th participant. There are no duplicate scores.

        Return an array with the rank obtained by each participant, giving medals to the
        first three.
        """
        raise NotImplementedError("Not implemented yet")
