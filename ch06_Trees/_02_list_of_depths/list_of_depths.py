from typing import List, Optional
from ch06_Trees._00_binarytree.node import Node

class ListOfDepths:
    def list_of_depths(self, root: Optional[Node]) -> Optional[List[List[Node]]]:
        """
        Implement an algorithm that returns a list containing the nodes of each level (a list of lists).
        If the tree has N levels, you must return N lists.

        Example:
          Input:
                4
             2     7
           1   3  6  9
          Output:
            [[4], [2, 7], [1, 3, 6, 9]]
        """
        raise NotImplementedError("Not implemented yet")
