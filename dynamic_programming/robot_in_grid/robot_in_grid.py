from typing import List, Optional
from dynamic_programming.robot_in_grid.cell import Cell

class RobotInGrid:
    def get_path(self, grid: List[List[bool]]) -> Optional[List[Cell]]:
        """
        A robot is located in the upper-left corner (0,0) of an m x n grid. In the grid, there are cells that are
        passable (True) and non-passable (False). Find a valid path for the robot to reach the bottom-right
        corner, knowing that it can only move down and to the right.
        """
        raise NotImplementedError("Not implemented yet")
