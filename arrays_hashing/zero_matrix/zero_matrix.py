from typing import List

class ZeroMatrix:
    def zero_matrix(self, matrix: List[List[int]]) -> None:
        """
        Given a matrix, write an algorithm to set zeroes in row R and column C if a
        0 exists in cell R:C (modify the matrix in-place).

        Example:
          Input:
            [2, 1, 3, 0, 2],
            [7, 4, 1, 3, 8],
            [4, 0, 1, 2, 1],
            [9, 3, 4, 1, 9]
          Output:
            [0, 0, 0, 0, 0],
            [7, 0, 1, 0, 8],
            [0, 0, 0, 0, 0],
            [9, 0, 4, 0, 9]
        """
        raise NotImplementedError("Not implemented yet")
