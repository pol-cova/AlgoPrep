from typing import List

class RedundantConnection:
    def find_redundant_connection(self, edges: List[List[int]]) -> List[int]:
        """
        Given a graph formed by adding an edge between two nodes of a tree, return
        the edge that can be removed to turn the graph back into a tree.

        If there are multiple solutions, return the last edge in the input.
        """
        raise NotImplementedError("Not implemented yet")
