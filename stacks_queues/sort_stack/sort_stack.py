from typing import List

class SortStack:
    def sort(self, stack: List[int]) -> List[int]:
        """
        Sort a stack such that the smallest items are on the top.
        You can use an additional stack (represented as a list), but you are not allowed to copy
        the elements into any other data structure.

        Example:
          Input: (top) 4->2->5->1
          Output: (top) 1->2->4->5
        """
        raise NotImplementedError("Not implemented yet")
