from typing import List, Optional
from ch14_ExtraProblems._01_merge_k_sorted_lists.node import Node

class MergeKSortedLists:
    def merge_k_lists(self, lists: List[Optional[Node]]) -> Optional[Node]:
        """
        Given an array of linked lists, each sorted in ascending order,
        combine them to return a single sorted linked list.
        """
        raise NotImplementedError("Not implemented yet")
