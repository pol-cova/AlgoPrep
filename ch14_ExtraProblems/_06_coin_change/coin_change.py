from typing import List

class CoinChange:
    def coin_change(self, coins: List[int], amount: int) -> int:
        """
        Given an array of integers representing the different denominations of coins available,
        returns the minimum number of coins needed to sum to a target amount.

        If it is impossible to reach that amount, returns -1.
        """
        raise NotImplementedError("Not implemented yet")
